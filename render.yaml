services:
  - type: web
    name: core4ai-backend
    env: python
    region: oregon
    plan: starter
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
